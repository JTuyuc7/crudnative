import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
    titulo: {
        textAlign: 'center',
        marginTop: 20,
        marginBottom: 30,
        fontSize: 25,
        fontWeight: 'bold'
    }
});

export default globalStyles;